# 更改记录 (CHANGELOG)

本文件记录 NC 刀路查看器 Qt 离屏渲染实验版 (原型-embed) 的所有更改。
> **维护约定**：每次代码/文档/打包有改动，都应在「未发布」或当次版本条目下追加记录。
> - 记录格式：`- <类型> <一句话说明>`，类型取 `新增 / 修改 / 修复 / 移除 / 文档 / 打包 / 测试`。
> - 改动较大或对外发布时，新建版本号条目 `[x.y.z] - YYYY-MM-DD`。
> - 遵循 TDD：先写/跑测试（green），再打包。

### 修复
- **播放时刀具落后于刀路**（复位后播放，刀具慢于轨迹出现）：
  - 根因一：文件行号不连续（头注释/空行）时，播放按行号+1 推进，当前行落在**无移动的行**上，`position_at_line` 返回旧位置 → 刀具/十字线停留旧处；改为 `_sync_move_line` 把行号**修正到 ≥目标行的第一个有移动行**（段内钳制）。
  - 根因二：前导起始进给（从原点出发）在离屏渲染**未被跳过**，画面从原点拉线到刀路起点；`set_result` 新增 `lead_skip` 参数（viewer 传 `_lead_skip`，render 数据版本 key 同步纳入）。
  - 根因三：轨迹模式刀具位置用 `position_at_line(当前行)`，与按移动裁剪的刀路不同步；改为**钉在刀路最后移动的终点**（`trace_drawn-1` 移动终点），与已画刀路永远同点。
- **Tk/Qt 双轨渲染打架**（排查清理）：
  - `_draw_axes`/`_draw_current`/`_draw_crosshair` 曾被改成纯空壳，**连 Tk 回退渲染的标记绘制一起废掉**（OpenGL 不可用时轴/当前行标记全丢）；恢复完整 Tk 实现 + `if self._qt_ready: return` 守卫（Qt 接管不画 Tk 图元，fallback 正常）。
  - render() Qt 分支清理 tag 列表**缺 `axes`/`axescorner`**，fallback 轴图元残留画布与离屏图片混叠；已补全。
  - `_trace_redraw` 重复渲染（`_trace_draw_to` 后 `_draw_tool_model` 再触发一次 `_qt_render`，同帧渲染两遍）；`_draw_tool_model` Qt 分支改为只同步刀具数据（渲染由调用方统一），`_clear_current` 显式补渲染。
### 测试
- 新增 4 例（Qt 接管时 Tk 标记 no-op、fallback 标记正常绘制、render 清理 fallback 轴残留、播放行号修正到移动行）；测试总数 **175**，py38 / py311 双环境全绿。
### 打包
- **字节码加密（PyInstaller `--key`）**：AES 加密全部 `.pyc`——解包后是密文，反编译工具失效（实测源码特征字符串无明文，仅模块导入名元数据不可避免）；`tinyaes` 依赖自动安装；Win7 目标流程不变（conda + UCRT + PyInstaller 5.x，DEPRECATION 提示为 6.x 移除预告，本版固定 5.13.2 不受影响）。
- **密钥与打包产物放一起**：`NCVIEWER_KEY` 环境变量覆盖 > 每次打包自动生成（`secrets.token_hex(16)`）> 保存到 `dist\.ncviewer_key`（与 EXE 同目录，`dist/` 已 git 忽略不入库）。
### 新增
- **窗口图标各级页面生效**：新增 `_icon_path`/`_set_icon`（打包后从 `_MEIPASS` 读内置图标，开发时读 `assets\NCodeViewer_icon.ico`，静默容错）；主窗口 + 全部二级窗口（程序详情/F 进给趋势/刀具剖面图/刀具自定义）标题栏均显示图标。**根因**：原打包仅 `--icon`（EXE 文件图标），运行时窗口 `iconbitmap` 找不到图标文件静默失败；`build_exe.bat` 新增 `--add-data` 把图标内置进 EXE。
### 测试
- 测试总数 **177**，py38 / py311 双环境全绿。
### 修复
- **播放经过隐藏 G0 时刀路瞬间清空**（十字线/刀具跟不上刀路）：`trace_drawn` 指向的移动若被过滤（隐藏 G0/前导，不在 `seg_map`），原 `seg_map.get(trace_drawn-1)` 返回 0 → 刀路清零而刀具/十字线停在 G0 终点，三者不同步；新增 `_seg_of_move` 统一回退到**最近一个未过滤移动**的段数/终点（刀路裁剪、刀具位置、十字线位置三处一致），实测经过 G0 移动时刀路保持不清空、刀尖同步。
- **当前段高亮缺前导/段过滤/隐藏 G0 检查**：Qt overlay 曾直接用 `move_by_line[当前行]` 高亮当前段——前导跳过段/被段过滤段/隐藏 G0 的刀路不显示却会被高亮（高亮一条不存在的刀路）；改为 viewer 在 `_qt_render` 统一判定（`_move_index` 查索引 + 前导/`_move_filtered`/`show_g0` 检查），把可高亮移动传给渲染器（`highlight_move` 参数，替代原 `move_by_line`）。
- **十字线不跟随刀尖（停在 0,0,0）**：轨迹模式下十字线/当前点位置用 `position_at_line(当前行)`，行号落在注释/空行上时返回旧位置（原点）；改为**轨迹模式跟随刀尖**（`trace_drawn` 最后移动终点，与刀具/已画刀路同步），非轨迹模式仍跟随当前行。实测十字线竖线/横线中心与刀尖投影一致（±2px 内）。
- **轨迹模式勾掉"显示刀具"仍显示刀具**：轨迹模式（播放/演示后）的开关切换走 `_trace_redraw` → `_qt_render`（不经过 `_qt_sync_data`），渲染器 `_show_tool` 未更新；`_qt_render` 改为**每次渲染前同步刀具状态**（`set_tool` 仅属性赋值无副作用），所有渲染路径生效。
### 测试
- 新增 1 例（当前段高亮检查：前导跳过段/隐藏 G0 不传高亮移动）；测试总数 **177**，py38 / py311 双环境全绿。
### 测试
- 新增 1 例（轨迹模式切换显示刀具开关，渲染器同步关闭/恢复）；测试总数 **176**，py38 / py311 双环境全绿。
### 修改
- **演示到行与播放同速**：演示动画曾用「剩余距离 1/80」恒定步长（远目标每帧推进十几行，明显快于播放的 batch 行/帧）；改为**与播放一致**——每帧合并推进 batch 行（同一速度档、同样行号修正），到达目标即停。
### 修复
- **3D 刀具侧面"只有中间一片"**：旧填充为**过轴心剪影薄片**（y=0 平面），斜视角投影成窄条、外表面透明（凹轮廓刀具更甚）；改为**旋转体外表面网格填充**（轮廓绕轴 24 段生成表面三角形，圆柱面/球头/刀柄全部上色），侧面/斜视角显示完整外表面实体，实测覆盖与描边轮廓一致。
### 测试
- 演示测试改为「与播放同速」（每帧 batch 行 + 到达即停）；测试总数 **175**，py38 / py311 双环境全绿。
### 修改
- **XYZ 方向指示器**：改为固定画布左下角（带轴端实心箭头，随旋转变化、始终可见）——原画在世界原点，模型适配后原点常贴画布边缘/出画布，标识看不到；轴文字标签（X/Y/Z，加粗 Consolas）由 QPainter 叠加到渲染帧（GL 立即模式无文字 API）。
- **世界原点坐标系**：(0,0,0) 处另绘一组三轴坐标系（轴长随缩放 = 世界尺度，最小 60px，**大于左下角指示器** 34px；线宽 2、箭头 10px、文字 12px），刀路定位点附近始终可见；世界原点保留白点标记。
- **加工方向箭头改镖形**：纯三角（14px）改为**镖形**（尖端+双翼+尾部内凹，GL 三角形扇绘制），放大到 **28px** 更醒目。
### 修复
- **3D 刀具半透明（挖空感）**：原为纯线框（轮廓+截面+经线），背景刀路直接透出；改为**剪影三角扇填充**（右缘+顶部直径+左缘，不自交）+ 独立**顶面圆盘填充**——斜视角实体完整不透明、有立体顶面（俯视视角刀具侧面几何不可见，显示顶面圆+轮廓线属正常）。后续完善：**填充色调亮**（实体 #8f8f99 / 顶面 #a8a8b2，与深色背景拉开对比，避免"深灰实体在深灰背景上近乎不可见"的假透明感），**去掉经线与截面圆线框**（只留剪影描边 + 顶面圆线 + 轴线，消除"很多外框线"观感）。
### 测试
- 测试总数 **171**（含像素级渲染帧断言），py38 / py311 双环境全绿。
### 修改
- 本实验版由主程序 `NCodeViewer` 1.1.0 分叉而来：**界面与交互完全保持主程序 Tk 实现**（三栏布局、播放控制、段过滤、搜索定位、统计侧栏、刀具面板等），仅把**画布刀路渲染**替换为 Qt 离屏 OpenGL 渲染（`qt_offscreen.py`）。
- **渲染架构**：`QOffscreenSurface + QOpenGLContext + QOpenGLFramebufferObject` 离屏渲染 → `QImage` 读回 → `PIL/ImageTk.PhotoImage` → Tk canvas 图片项显示；`_qt_ready` 就绪前（after 150ms）或 OpenGL 不可用时自动回退主程序 Tk 渲染。
- **全量刀路 VBO**：加载/段过滤/G0/刀具变化才重建（data 版本 key），旋转/缩放/平移只更新 MVP 重渲染；合并折线（同色连续、共享端点），**不抽稀、不断线**。
- **overlay 移至 Qt 立即模式**：XYZ 轴、当前行高亮/方向箭头、十字线、3D 刀具模型（刀尖对刀、直径投影放大判定）全部由 `qt_offscreen` 绘制，Tk canvas 不再有刀路/标记图元。
- **移动级轨迹渐进**：播放/单步按**移动**逐步增长刀路（"走到哪显示到哪"，`_move_seg_map` 逐移动精确映射段数，渲染按 `trace_drawn` 裁剪顶点）；合并跳行仍为行级推进语义，渲染回推到该行最后移动。
- **PhotoImage 复用**：播放每 tick 复用同一 PhotoImage（`paste` 更新像素），Tk 图片对象不累积（修复越用越卡）；画布尺寸变化时按新尺寸重建。
- 测试适配 Qt 离屏：主程序 163 例中涉及 Tk 画布图元/回退渲染机制的用例改为断言离屏渲染路径（`_trace_drawn` 状态、`_qt_render` 触发、`_renderer` 状态传递）；新增 `test_qt_offscreen.py` 8 例（全量 VBO 不抽稀、seg_map 逐移动精确、G0 过滤、段过滤、空程序、渲染帧、FBO 重建、trace 裁剪）。**py38 / py311 双环境全绿（170 例）**。
### 修复
- **seg_map 计数 bug（"一段颜色执行完才显示"）**：合并进同色折线的移动，段数只在颜色段切换（折线 flush）时计入 → 播放到颜色段内部时整段不显示，颜色段结束才一起出现；改为每移动完成后 = 已落库段数 + 当前折线段数，逐移动精确。
- **播放段尾不停止**：段模式播放钳制在段内最后移动后停止（`_play_tick` 按 `_seg_line_bounds` 计算终点）。
- **`_fill_code` 全删文件崩溃**：`self.result` 为 None 时访问 `.moves` 崩溃，补空结果保护。
- **`show_tool` 切换不生效**：render 数据版本 key 漏 `show_tool`，开关切换后 overlay 不更新；已加入 key。
- **Tk 回退渲染残留图元**：`_qt_ready` 就绪前早期渲染产生的 Tk 刀路/标记图元残留画布（与离屏图片混叠）；Qt 接管渲染时统一清理。
- **`_play_tick` 合并跳行语义**：初版把合并数当移动数推进（行内多移动时跨行）；改回行级推进 + 渲染按移动裁剪。

### 文档
- 按主程序规格建立完整项目结构并撰写本文档目录下全套文档（README、技术手册、开发流程、本文件）。

---

## [0.1.0] - 2026-08-09

本实验版定位：验证「Qt OpenGL 渲染嵌入 Tk 界面」方案，目标在完整刀路下实现 60fps 交互，且界面完全保持主程序 Tk 实现。
### 新增
- 从主程序 `src/nc_viewer` 复制 `parser / geometry / theme / tool / viewer`（viewer 改造接入离屏渲染），新增 `qt_offscreen.py`。
- 离屏渲染链路：`QOffscreenSurface + QOpenGLContext(2.0 compatibility, 4x MSAA) + QOpenGLFramebufferObject` → 每帧渲染 VBO 全量刀路（GL_LINES）+ 立即模式 overlay → `FBO.toImage()` → `convertToFormat(RGB888)` → `PIL Image.frombuffer`（按 `bytesPerLine` stride 逐行，防 45° 斜线）→ `ImageTk.PhotoImage` → canvas `create_image`。
- 渲染性能：data 版本 key（result/段过滤/G0/刀具）不变则 VBO 复用，旋转/缩放/平移仅重渲染图片；`_view_refresh_soon` 合并拖动刷新；**不抽稀、不断线**（延续主程序约束）。
- 轨迹渐进：移动级裁剪渲染（`_move_seg_map`：移动索引 → 完成后的累计段数），播放推进维护 `_trace_drawn`，渲染 `glDrawArrays` 裁剪到该移动边界。
- 播放/单步/复位/绘制到结尾/直达/演示、平移、缩放、适配、段过滤、G0 开关、刀具开关在离屏渲染下全部接通。
### 移除
- 主程序 Tk 画布刀路/overlay 渲染路径（回退保留于 `_qt_ready=False` 时）。
- 原 `qtgl.py`（原型-qt 的 QOpenGLWidget 嵌入方案，已被离屏方案取代）。
### 文档
- 初版：README、技术手册、开发流程、本文件（对齐主程序 1.1.0 规格）。
