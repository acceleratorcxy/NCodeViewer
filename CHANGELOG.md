# 更改记录 (CHANGELOG)

本文件记录 NC 刀路查看器的所有更改（功能、修复、重构、打包等）。
> **维护约定**：每次代码/文档/打包有改动，都应在「未发布」或当次版本条目下追加记录。
> - 记录格式：`- <类型> <一句话说明>`，类型取 `新增 / 修改 / 修复 / 移除 / 文档 / 打包 / 测试`。
> - 改动较大或对外发布时，新建版本号条目 `[x.y.z] - YYYY-MM-DD`。
> - 遵循 TDD：先写/跑测试（green），再打包。

### 新增
- **窗口标题栏图标**：主窗口与全部二级窗口（详情/F 曲线/剖面图/刀具自定义）统一显示程序图标（`assets\NCodeViewer_icon.ico`）——`iconbitmap` 设置（Windows 句柄存储，以设置无异常判定成功）；开发/打包双模式资源定位（`sys._MEIPASS` / 项目 `assets`）；EXE 文件图标与窗口图标资源一并打包（`--icon` + `--add-data`）。
### 打包
- **字节码加密（PyInstaller `--key`）**：AES 加密全部 `.pyc`——`pyinstxtractor` 解包后是密文，`uncompyle6` 等反编译工具失效（实测源码特征字符串无明文）；`tinyaes` 依赖自动安装；Win7 目标流程不变（conda + UCRT + PyInstaller 5.x）。
- **加密密钥随产物存放**：每次打包重新生成（EXE 自包含解密，密钥无需跨版本一致）并保存到 `dist\.ncviewer_key`（与 EXE 同目录、不入库、每次打包更新）；`NCVIEWER_KEY` 环境变量可覆盖。
### 修改
- **刀具 3D 模型完善**：
  - **侧壁 128 条带明暗渐变**（相邻色差 ~3/255 绝对无分段；法向·固定光源定灰阶 45~195 金属对比 + 背面剔除 + **条带描边同色**无背景细缝）——覆盖全圆周，侧面/俯视均为完整柱面立体；
  - **去除所有外框线**（含外轮廓描边——不再有"外表皮"线框感），表面由条带自身构成；顶面圆盘按光源受光、底部圆盘暗色闭合（平底刀不镂空），刀尖标记保留。
- **原点坐标系放大至 90px**（原 60px，轴端箭头 12px、加粗标签 11 号）；左下角指示器保持 34px。
- **刀路方向镖形箭头放大至 28px**（原 20px，翼展 ~35px），坐标轴端小箭头同步放大。
### 测试
- 更新 2 例（镖形 28px 跨度上限 26→38、坐标系主轴 48→70）；新增 1 例（刀具面构成：明暗半面+顶面+平底刀底部圆盘、球头无底、无外框线）；测试总数 **166**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含刀具完善/坐标系与箭头放大，约 10.7 MB），冒烟通过。
### 优化
- **渲染拖帧提速约 2 倍**（43k 段样例实测 45ms → 22ms，~50fps；不抽稀/不断线）：
  - **预计算渲染元数据**（`_build_render_meta`）：可见性/颜色/合并标记按移动查表，热循环零字典/属性/切片访问（加载/段过滤/G0 开关变化时毫秒级重建）；
  - **合并段跳过共享端点重投影**：同色连续移动只投影首点之外的点（43k 段中 42.7k 段可合并，投影点数减半）；
  - **拖动中不写投影缓存 base**（缓存只在释放后的最终帧写）。
### 测试
- 新增 3 例（渲染元数据合并/可见性正确性含 G0 隐藏重建、折线合并结构、拖动帧不写缓存）；测试总数 **165**，py38 / py311 双环境全绿。
### 修改
- **XYZ 坐标系**：原点 (0,0,0) 处绘制完整坐标系（三轴 + 轴端箭头 + 加粗标签 + 原点白圈，固定 60px）；画布**左下角新增小方向指示器**（34px，锚定画布、平移/缩放不动），作模型适配后原点出画布时的保底方向参照——**原点坐标系大于左下角指示器**。
- **加工方向箭头改镖形**：纯三角（14px）改为**镖形**（尖端+双翼+尾部内凹，20px，1.5px 深色描边），更大更醒目；坐标轴端小箭头（9px/7px）同规格。
### 修复
- **3D 刀具半透明（挖空感）**：实体原为环带多边形（右缘+顶部圆+左缘），斜视角投影自相交，Tk 默认 even-odd 填充把重叠区镂空成洞 → 背景/刀路透出（Tk 8.6.9 无 fillrule 选项可换绕线规则）；改为**剪影**（右缘+顶部直径+左缘，不自交）+ 独立**顶面圆盘**填充，实体完整且更有立体感。
### 测试
- 更新 1 例（方向箭头固定尺寸规格：镖形 20px 级，跨度上限 20→26）；新增 1 例（原点坐标系/左下角指示器：三轴齐全、原点轴长 > 左下角轴长、平移不动左下角指示器）；测试总数 **162**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含镖形箭头/坐标系/刀具剪影，约 10.7 MB），冒烟通过。
### 修改
- **加工时间首条按抬刀平面起算**：从机器原点 (0,0,0) 出发的首条移动（程序第一条），起点按抬刀平面 (0,0,首条终点Z) 计算——只算水平进给距离，**不含从地面抬升的虚拟段**（`move_time_sec` 新增 `lift_start` 参数，`compute_machining_time` 与播放进度时间轴自动应用于首个参与移动）。
- **加工时间显示向上取整到秒**：5891.5s 显示 1:38:12（而非四舍五入的 1:38:11）。大样例实测显示 **1:38:12**。
### 测试
- 新增 1 例（首条抬刀平面水平起算、非原点首条不受影响、start 后首个移动同样应用）；测试总数 **163**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含加工时间抬刀平面口径，约 10.7 MB），冒烟通过。
### 修复
- **画布 3D 刀具模型**：
  - **旋转中突然变大**：放大判定原用「轴向投影高度」（刀具轴接近平行屏幕时投影趋零 → 放大 8 倍爆炸）；改为**直径投影**——旋转体任意角度投影宽度 = 直径（与视角无关），放大因子稳定，上限收紧 8→6；
  - **部分角度变透明（挖空感）**：关键截面椭圆原用深色填充（比实体深，放大后像内部挖洞）；改为**浅色高光填充**（比实体浅，立体渐变感）；
  - 顺带修复 `_draw_current` 潜在未定义变量崩溃（当前行是隐藏 G0 时 `idx` 未赋值）。
### 测试
- 新增 2 例（大刀具各角度不放大 + 截面高光、小刀具放大到可见尺寸）；测试总数 **161**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含 3D 刀具模型修复，约 10.7 MB），冒烟通过。
### 优化
- **F 曲线滚动条拖动卡顿**（定位：阶梯线长垂直跳变 + 5px 描边在 GDI 渲染极慢，完整帧 48ms）：
  - **拖动中轻量粗绘**：滚动条拖动期间用无描边 + 抽稀加倍的重绘（完整帧 48ms → 9ms，提速 81%），停止 300ms 后自动恢复精绘（描边 + 全量抽稀）；
  - 抽稀收紧到每像素 1 个数据点（阶梯线每点 2 坐标）；滚动条显示状态变化才 pack（消除重复布局）。
### 测试
- 新增 1 例（拖动粗绘无描边 + 停止后自动精绘双线）；测试总数 **159**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含 F 曲线拖动优化，约 10.7 MB），冒烟通过。
### 修复
- **F 曲线斜线问题**：F 值是阶跃变化的，原折线连接相邻点会画出"渐变斜线"（表示 F 过渡，与瞬间切换不符）；改为**阶梯线**绘制——每个 F 值保持水平线段到下一数据点时间、垂直跳变到新 F，全程无斜线；行号/时间横轴与抽稀均适用。
### 测试
- 新增 1 例（阶梯线结构：水平保持 + 垂直跳变同 x）；测试总数 **158**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含 F 曲线阶梯线，约 10.7 MB），冒烟通过。
### 优化
- **F 曲线界面性能**（真实 25321 点样例）：
  - **数据抽稀**：绘制时每像素最多 2 个点（全览 25321 → ≤1600 点），`create_line` 大点数开销 6.1ms → 0.5ms（12 倍）；缩放后视口内点少自动不抽稀保持精确；
  - **滚轮/滚动条防抖**：连续滚动事件合并为一次重绘（80ms），不再逐事件全量重绘；视口计算即时更新、无变化时跳过调度。
  - 实测：单次重绘 4.6ms，20 连滚仅触发一次重绘，操作流畅。
### 测试
- 新增 2 例（大数据抽稀点数受控、滚轮防抖合并重绘）；测试总数 **157**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含 F 曲线性能优化，约 10.7 MB），冒烟通过。
### 修改
- **F 进给趋势页横轴增强**：
  - **单位切换**：底部配置栏新增 秒/分钟/小时 单选，刻度数值按单位换算（分钟=÷60、小时=÷3600），轴标签随单位；
  - **滚轮横向缩放**：时间轴上滚轮以鼠标 x 位置为锚放大/缩小视口（向上=放大细节、向下=缩小），刻度间隔按视口跨度自动取整（1/2/5/10/30/60…秒）；
  - **横向滚动条**：缩放后出现，可拖动平移时间轴视口（`moveto`/翻页/逐档），全览时自动隐藏；行号模式不缩放、无滚动条。
### 测试
- 新增 2 例（单位切换刻度/标签、滚轮锚点缩放 + 滚动条平移 + 行号模式不生效）；测试总数 **155**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含 F 曲线横轴缩放/单位/滚动条，约 10.7 MB），冒烟通过。
### 修改
- **F 进给趋势页完善**：
  - **横轴改为加工时间**（累计秒，含 G0 快移时间，数据点 = 切削移动完成时刻）；窗口底部新增配置栏可切换**行号**横轴；
  - 绘制质量提升：双线绘制（亮色描边 + 主色曲线）、横 8 档/纵 6 档网格、余量充足（横轴标签 mm:ss/h:mm:ss 自动）、点数 ≤200 时标注数据点；
  - 仍为单条连续折线、无档位色块图例。
### 测试
- F 曲线测试更新为 (行号, 累计秒, F) 三元组（时间数值精确断言）+ 新增横轴切换/刻度格式 1 例；测试总数 **152**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含 F 曲线完善，约 10.7 MB），冒烟通过。
### 修改
- **刀具剖面图尺寸标注按工程制图规范重绘**（放大窗口）：尺寸界线从轮廓引出、尺寸线两端**实心三角箭头朝外**（此前 45° 斜线且右端/下端箭头方向反了）、文字在尺寸线上方（水平标注）或左侧（垂直标注）。
- **画布内 3D 刀具模型凝实化**：由半透明（stipple 抖动）改为深灰**实心填充 + 亮轮廓**，关键截面圆投影为实心椭圆增强立体感。
### 测试
- 新增 2 例（水平/垂直标注箭头方向朝外、3D 模型实体无 stipple）；测试总数 **151**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含剖面图规范标注 + 3D 刀具实体化，约 10.7 MB），冒烟通过。
### 新增
- **程序加工时间统计**：程序统计面板新增「加工时间」行——按各移动路径长度/速度自动计算（直线取端点距、圆弧取弧长；切削用 F mm/min、G0 按默认快移速度 20000 mm/min），段模式下为勾选段内统计；**画布点选某段刀路时统计显示该段时间**，点空白/取消查看恢复默认。
- **播放加工时间进度**：播放/演示/单步时顶部栏右上角进度条按加工时间比例显示当前进度（当前行累计加工时间/可见总时间），旁边显示百分比；暂停保持、播放到结尾或复位后隐藏；段过滤变化后时间轴自动重建。
### 测试
- 新增 7 例（直线/G0/圆弧单移动时间、总时间与段范围、统计行显示、拾取段时间与恢复、播放进度百分比与复位隐藏）；测试总数 **149**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含加工时间统计 + 播放加工时间进度，约 10.7 MB），冒烟通过。
### 新增
- **文件列表右键菜单**（MPF/APT 双列表均支持多选）：
  - **删除所选文件**：从内存缓存移除选中项并刷新；当前显示文件被删时自动切换剩余首个程序（全删则清空主视图）；APT 删除不影响 MPF。
  - **配对 APT 刀具**（数控程序列表右键）：菜单项展开**二级子菜单列出全部已加载 APT**，点击某个 APT 即把其刀具带给选中的程序，并建立双向关联高亮；APT 无刀具定义时提示，无已加载 APT 时子菜单为禁用占位项。
### 修复
- `_fill_code` 空行列表时 `insert("end")` 缺参数崩溃（全删文件清空视图路径触发）。
### 测试
- 新增 2 例（删除多选+主视图切换/清空、手动配对带刀具+partner）；测试总数 **142**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含文件列表右键菜单，约 10.7 MB），冒烟通过。
### 修改
- **图例只显示程序实际存在的对照**：程序无 G0 移动时不显示「G0 快速移动」图例项。
- **F 进给趋势图改为单条连续折线**：全部数据点顺序连接成一条折线（主题蓝），不再按 F 值分色分段，去掉右上角档位色块图例；Y 轴刻度仍反映 F 数值。
### 测试
- 新增 2 例（无 G0 图例隐藏/有 G0 显示、F 曲线单条折线无档位图例）；测试总数 **140**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含图例按需显示 + F 曲线单折线，约 10.7 MB），冒烟通过。
### 修改
- **加载进度改为顶部栏内嵌显示**：不再弹出独立进度窗口，改为在主窗口顶部栏右上角内嵌「文字 + 进度条」（超 80ms 才显示防闪烁，加载结束自动隐藏，不打断操作、不弹模态）。
- **命令行支持多文件参数**：`NCViewer.exe 文件1 文件2 …` 与界面多选等效，一次加载多个文件。
### 测试
- 进度测试更新为内嵌断言（显示/隐藏/不产生 Toplevel）；测试总数 **138**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含顶部栏内嵌加载进度，约 10.7 MB），冒烟通过。
### 修复
- **加载进度弹窗进度条为浅色**：`ttk.Progressbar` 此前未配置样式，clam 主题默认条/槽为浅灰白（`#dcdad5`/`#bab5ab`），大 MPF 文件加载时弹窗里出现白色进度条；现配置为深色（槽 `INPUT_BG`、条 `ACCENT` 蓝）。
### 测试
- 新增 1 例（进度条 troughcolor/background 深色断言）；测试总数 **138**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含深色进度条，约 10.7 MB），冒烟通过。
---

## [1.1.0] - 2026-08-08

本次会话为项目增加 Windows 7 支持：改用 conda 的 Python 3.8 构建 EXE（3.9+ 官方不支持 Win7），并内置 UCRT 运行时使裸装 Win7 可运行。
### 新增
- 支持 Windows 7（64 位）目标：`build_exe.bat` 自动探测 conda `python38` 环境（Python 3.8.19，自带 Tk 8.6.9）作为构建解释器，Win10/11 构建不受影响（回退 `py -3.11` / `py -3.14`）。
- conda 构建时自动把 UCRT（`ucrtbase.dll` + `api-ms-win-crt-*.dll` + `vcruntime140*.dll`）打包进 EXE，裸装 Win7 无需 KB2999226 补丁。
- 打包脚本支持 `NCVIEWER_PY` 环境变量显式指定构建解释器（python.exe 完整路径）。
### 修改
- `pyproject.toml`：版本提升至 1.1.0；`requires-python` 放宽为 `>=3.8`；dev 依赖限定 `pytest<9`（pytest 9 不支持 3.8）。
- 维护 `build_exe.bat`：重构为四步流程（解释器探测 → PyInstaller 检查 → 清理 → 打包），conda python38 构建时自动固定 PyInstaller 5.13.2（6.10+ 的 bootloader 不支持 Win7）。
- 修复 `build_exe.bat` 两个 cmd 陷阱：以引号开头的命令行若以重定向结尾会被剥首尾引号（一律改重定向前置、输出走临时文件）；块内 echo 文本不得含半角括号（会被当作分组操作符）。
### 移除
- 删除 `requirements.txt`（与 `pyproject.toml` dev 依赖重复）。
- 删除 `NCViewer.spec`（构建以 `build_exe.bat` 为准，PyInstaller 每次运行自动重新生成，无用途），`.gitignore` 新增 `*.spec`。
- 清理全部缓存与构建残留：各目录 `__pycache__`/pyc（含旧扁平模块 `nc_parser`/`nc_viewer`/`test_nc_*` 及 `f_detect` 实验残留）、`.pytest_cache`、`build/`、`src/nc_viewer.egg-info`。
### 文档
- 全面校对并重写 `README.md`、`docs/技术手册.md`、`docs/开发流程.md`：修复历史乱码字符、损坏的目录树与编号错乱。
- 修正技术手册事实性错误：「旋转中心 2px 内吸附」改为「12px」（与 `PICK_MAX_PX=12` 一致）。
- 更新文档目录结构（移除 `requirements.txt` / `NCViewer.spec`，补充 `样例文件`、`__init__.py` 职责说明）。
### 新增
- 建立 git 仓库；`.gitignore` 排除 `样例文件/`（仅本地测试用）与 `docs/本地版/`（含本机路径等隐私信息，不入库）。
- 文档拆分为发布版与本地版：`docs/`（发布版，本机路径用 `<conda38-python>` 占位符替代，随仓库分发）与 `docs/本地版/`（恢复本机路径，仅供本机使用，被 git 忽略）。
### 修改
- 样例文件不再打包进 EXE：`build_exe.bat` 移除 `--add-data "样例文件;样例文件"`；`viewer._sample_dir` 打包环境回退到用户主目录作为文件对话框初始目录（样例仅本地开发/测试用）。
- `build_exe.bat` 的 conda python38 探测改为 `where conda` 动态定位 conda 根目录（移除本机硬编码路径，不泄漏本机路径信息）。
- 重建 `dist\NCViewer.exe`：**10.5 MB**（原 30.3 MB，样例文件约占 20 MB），归档验证无样例条目、UCRT/tcl/tk 完整。
### 打包
- EXE 使用 `assets\NCodeViewer_icon.ico` 作为程序图标（`build_exe.bat` 增加 `--icon`，产物约 10.6 MB），冒烟测试通过。
### 测试
- 新增 `_sample_dir` 目录定位与回退测试；测试总数 **39**（解析 28 + 几何 7 + 界面结构 4），py38 / py311 双环境全绿。
### 新增
- 深色现代化主题模块 `src/nc_viewer/theme.py`：ttk clam 定制（VS Code 风格深色调色板 + Segoe UI 字体），按钮悬停高亮（clam active 状态，无需事件绑定），纯标准库，兼容 Win7 / Tk 8.6.9。
### 修改
- `viewer.py` 全面接入深色主题：画布/代码区/文件列表/输入框/滚动条/标签统一配色，「打开文件…」主按钮强调色，代码行号与当前行高亮适配深色。
### 测试
- 新增主题测试 3 例（clam 已应用、画布背景用主题色、颜色常量格式）；测试总数 **42**（解析 28 + 几何 7 + 界面结构 7），py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含深色主题与图标，约 10.6 MB），冒烟测试通过。
### 新增
- 侧栏「程序统计」面板：X/Y/Z 行程、S/F 范围、F 档位数、G0~G3 次数等关键指标；「详情…」二级窗口展示完整统计（行数/段数/各 F 档位段数与占比）。
- 「F 曲线」二级窗口：F 进给随行号变化趋势折线，线段按 F 档位调色板着色，min/max 参考虚线，右下角档位图例（X 轴=行号、Y 轴=F）。
- 颜色图例改为**横向流式排布**：按字体测量宽度自动换行（多行），任意 F 档位数自适应。
- 解析器新增 **S 主轴转速模态跟踪**（`Move.s`，规则同 F：G0 为 None）；新增 `compute_stats` 程序统计纯函数（行程按刀路端点计算，不含原点）。
### 测试
- 新增 8 例（S 模态 2、compute_stats 3、统计面板/F 曲线数据/图例 3）；测试总数 **51**（解析 33 + 几何 7 + 界面 11），py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含统计与曲线功能，约 10.6 MB），冒烟测试通过。
### 修改
- 侧栏改为**可滚动容器**（Canvas + 内嵌 Frame + 竖向滚动条 + 滚轮支持）：窗口缩小时内容完整可见，图例随侧栏滚动（不再压缩裁切）。
- **F 曲线窗口支持拉伸**：绘制逻辑抽为 `_draw_f_curve(cv, data, W, H)`，窗口 `<Configure>` 60ms 防抖重绘，内容跟随尺寸。
- **DPI 感知**：新增 `_enable_dpi_awareness()`（Win8.1+ 逐显示器 / Win7 系统级回退），修复高分屏缩放下 Tk 文字模糊（位图放大问题）。
### 测试
- 新增 3 例（侧栏滚动容器、F 曲线任意尺寸绘制、DPI 感知幂等）；测试总数 **54**（解析 33 + 几何 7 + 界面 14），py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含滚动/拉伸/DPI 修复，约 10.7 MB），冒烟测试通过。
### 新增
- **程序逐行运行**（代码区底部播放控制条）：连续播放/暂停（慢/中/快三档速度）、单步前进/后退、复位；点击代码行选中目标行（执行位置不动），「直达行」后台直接运算到目标行，「演示到行」动画逐帧推进（自适应步长，任意距离 ≤80 帧到达）。
- 动画**轻量渲染路径**：`set_current_line(animate=True)` 只更新当前行标记与代码高亮，不重画整幅刀路（大程序播放不卡）。
### 修改
- 点击代码行语义：从"立即跳转"改为"选中目标行"（独立高亮 + 控制条显示目标行号）；直接跳转仍可用行号/N 号输入框与搜索。
### 测试
- 新增 5 例（控制条按钮存在、目标行选中不动执行位、直达行、演示自适应步长、动画轻量路径）；测试总数 **59**（解析 33 + 几何 7 + 界面 19），py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含逐行运行功能，约 10.7 MB），冒烟测试通过。
### 修改
- **播放控制条移至画布下方**（3D 刀路画布与状态栏之间），不再位于代码区底部。
- **轨迹渐进显示**：播放/演示/单步/直达均从**空白画布**起，按程序逐行"画"出刀路（增量绘制，同色段合并追加，后退整段重绘），执行位置与已执行刀路同步；暂停/结束保留已绘轨迹，缩放/旋转/适配等操作恢复全量显示。
- 演示步长改为**恒定步长**（距离 1/80 向上取整），修复指数衰减导致无法精确到达目标的问题——任意距离 ≤80 帧精确到达。
### 测试
- 新增 2 例（控制条位于画布下方、轨迹渐进与后退重绘）；测试总数 **61**（解析 33 + 几何 7 + 界面 21），py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含轨迹渐进显示，约 10.7 MB），冒烟测试通过。
### 修复
- **播放中视图操作保持轨迹**：旋转/缩放/适配/窗口缩放不再触发全量渲染（改为 `_view_refresh`，轨迹模式下重投影已画轨迹），刀路不会"跳出来"。
- **播放中左键平移轨迹错乱**：`canvas.move` 后同步更新轨迹存储坐标，后续追加不再混用新旧坐标系。
### 测试
- 新增 2 例（轨迹模式下缩放/旋转保持轨迹、平移坐标同步）；测试总数 **63**（解析 33 + 几何 7 + 界面 23），py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含播放中视图操作修复，约 10.7 MB），冒烟测试通过。
### 修改
- **程序起点改为第一个可解析点**：新增 `_compute_lead_skip`，显示时跳过连续从原点(0,0,0)出发的起始进给段（机器对刀/接近段噪声）；全量渲染、轨迹播放、适配取景、段高亮统一生效，适配聚焦实际加工区域。解析位置跟踪不变。
### 测试
- 新增 2 例（前导跳过起始进给段、非原点起点不跳过）；测试总数 **65**（解析 33 + 几何 7 + 界面 25），py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含程序起点修正，约 10.7 MB），冒烟测试通过。
### 修改
- **方向箭头增强**：当前段加工方向箭头尺寸 9 → 14，翼展加宽并加深色描边，短段上仍按段长比例缩小。
- **播放合并跳行**：控制条新增「合并」输入（可编辑下拉：1-10 与 20-100 整十快捷项，支持手输任意 1-100），播放一次推进 N 行并同步合并绘制轨迹；非法输入回退 1，超界钳制到 100。
### 测试
- 新增 3 例（合并跳行推进、非法/超界钳制、箭头尺寸）；测试总数 **68**（解析 33 + 几何 7 + 界面 28），py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含箭头增强与合并跳行，约 10.7 MB），冒烟测试通过。
### 新增
- 新增 `src/nc_viewer/tool.py` 刀具几何模块：六种刀具类型（圆鼻/平底/反锥/铅笔刀(正锥)/普通钻头/中心钻）规格定义、CATIA APT `CUTTER/` 七参数解析（含 `$` 行续、锥角 a/b 位兼容、类型启发式推断）、剖面轮廓生成、摘要文本。
- **刀具自动解析**：加载 NC 时从同目录同名 `*_I.aptsource`（或文件本身为 aptsource）解析首个 CUTTER 刀具；自定义刀具优先于解析结果。
- **直径剖面图**二级窗口：镜像轮廓 + 尺寸标注（D 直径/L 刃长/θ 顶角锥角/R 圆角），可拉伸重绘。
- **刀具自定义**窗口：选择类型动态生成规格输入框，应用后刷新全部显示；支持恢复自动解析。
- **刀具 3D 模型**：画布内绘制旋转体模型（剪影+经线+截面圆+轴线），**刀尖对刀**锚定当前执行位置随播放移动，投影过小时按比例放大保证可见；顶部「显示刀具」开关与「显示G0快移」并列（无刀具时禁用）。
### 修改
- 布局：**按行定位/搜索定位面板移至 NC 代码区右侧**（与上方侧栏同列对齐，sash 可拖）；侧栏保留图例/刀具/统计/当前位置。
- 颜色图例下方新增刀具信息行（摘要 + 剖面图/自定义按钮）；程序统计面板新增「刀具」行。
### 测试
- 新增 17 例（tool 解析/几何/摘要 11、UI 刀具 6）；测试总数 **85**（tool 11 + 解析 33 + 几何 7 + 界面 34），py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含刀具功能，约 10.7 MB），冒烟通过，归档确认含 `nc_viewer.tool`。
### 修改
- **刀具解析补全刃长/总长**：刃长取 TOOLNO 第 5 位（实测 L70/L55/L52 等），总长取 CUTTER 第 7 参数（TOOLNO 第 4 位的 120 为刀柄标准长，非刀具实际总长）；反锥/钻头 TOOLNO 第 3 位为角度值时按逗号位置解析不塌缩。摘要增加刃长（如 `平底立铣刀 D20 R3 L70`）。
- **刀具 3D 模型改为半透明实体**：旋转体外轮廓多边形 + stipple 半透明填充 + 经线/截面圆表面细节 + 刀柄延伸至总长，不再是全透明勾线。
- **播放中切换「显示刀具」/「显示G0快移」不再退出轨迹模式**（开关命令改 `_view_refresh`），已画刀路保持渐进显示。
- **刀具栏移至当前位置信息栏下方**独立一栏（LabelFrame「刀具」，含摘要与剖面图/自定义按钮），不再位于颜色图例内。
### 测试
- 新增 4 例（TOOLNO 刃长/总长、反锥带角度字段、无 TOOLNO 回退、播放中切开关保持轨迹）；测试总数 **89**（tool 15 + 解析 33 + 几何 7 + 界面 34），py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含刀具修正，约 10.7 MB），冒烟通过。
### 修改
- **刀具类型命名对齐《刀具说明.txt》**：圆角立铣刀（含球头 R=D/2）统称**圆鼻立铣刀**（如 D20R3/D10R5 → 圆鼻），D10R0 → 平底；修复反锥刀（r>0 且锥角<0）被误判为圆鼻的问题（类型判断顺序调整）。
- **反锥立铣刀剖面修正**：TOOLNO 直径即锥顶直径，由 `(D-锥顶D)/2/tanθ` 反推锥段长（D12R3A2 → 刃长≈21.95，角度直观）；剖面含底部圆角 + 收窄锥段 + **锥段结束处缩颈**（刀杆对比切削刃明显变细）+ 刀柄至总长。
- 圆鼻立铣刀剖面区分球头（R=D/2 半球）与圆角（R<D/2 底部圆角+柱体）；摘要数值舍入至 3 位小数。
### 打包
- 重新打包 `dist\NCViewer.exe`（含反锥修正，约 10.7 MB），冒烟 3/3 通过。
### 修改
- **刀具剖面图内嵌显示**：直接绘制在侧栏「刀具」栏内（刀具信息下方，含缩颈刀柄与 D/L 紧凑标注），无需点击二级窗口；原「剖面图」按钮改为「放大」，打开带完整尺寸标注的大窗口。
### 测试
- 新增 1 例（内嵌剖面图绘制与清空）；测试总数 **90**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含内嵌剖面图，约 10.7 MB），冒烟 2/2 通过。
### 修改
- **刀具栏固定在侧栏底部**（滚动区之外，紧挨底部行按行定位面板上沿），剖面图画布拉长至 260px，随栏宽自适应。
### 打包
- 重新打包 `dist\NCViewer.exe`（含刀具栏布局调整，约 10.7 MB），冒烟通过。
### 修改
- **当前位置改为只读框字段展示**：X/Y/Z 各一个数值框（大号等宽字体 Consolas 12、深色底描边、只读），紧凑排列；本行信息单行显示（行号 + N 号/移动指令）。
- 纵向空间重分配：当前位置区域压缩，**刀具剖面画布拉高至 340px**。
### 测试
- 新增 1 例（只读框 X/Y/Z 数值）；测试总数 **91**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含当前位置美化，约 10.7 MB），冒烟通过。
### 修改
- **当前位置字段补全为 8 个只读框**：X/Y/Z/S/F/G/行/本行 自上而下排列（S/F/G 为当前行移动指令的模态值，本行显示原始代码行文本）。
### 测试
- 更新只读框测试（S/F/G/行/本行 断言）；测试总数 **91**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含位置字段补全，约 10.7 MB），冒烟通过。
### 修改
- 当前位置字段统一**左对齐**（与「本行」一致）。
### 打包
- 重新打包 `dist\NCViewer.exe`（字段左对齐，约 10.7 MB），冒烟通过。
### 修改
- **刀具栏顶部紧贴当前位置栏下边**：当前位置与刀具栏均固定于侧栏底部（滚动区仅剩图例+程序统计）。
- **程序默认最大化打开**（`state('zoomed')`，Win7 支持）。
### 测试
- 新增 1 例（当前位置/刀具栏同容器且不在滚动区）；测试总数 **92**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含布局与最大化，约 10.7 MB），冒烟通过。
### 修改
- **程序统计下边紧贴当前位置上边**（统计/当前位置/刀具三栏均固定于侧栏底部，滚动区仅剩图例）；**刀具剖面画布加高至 460px**。
### 打包
- 重新打包 `dist\NCViewer.exe`（侧栏三栏贴紧 + 刀具加高，约 10.7 MB），冒烟通过。
### 修改
- **侧栏恢复整体滚动**（图例/程序统计/当前位置/刀具 顺序固定，四节同在一个滚动容器）；统计/位置/刀具三节固定大小；**刀具剖面画布回到 340px**。
### 打包
- 重新打包 `dist\NCViewer.exe`（侧栏整体滚动，约 10.7 MB），冒烟通过。
### 新增
- **按段浏览**：段 = 刀路从抬刀平面下降到回到抬刀平面的完整循环；抬刀平面自动检测（出现次数 ≥2 的最高 Z 档位，排除偶发高 Z），**支持用户修改并自动重算分段**；面板位于搜索定位栏下方（上一段/下一段/段号跳转/段信息）。
- **段模式（仅显示当前段）**：画布只渲染当前段刀路，段内**播放/单步/复位/绘制到结尾**同步生效（钳制在段内）；关闭后恢复全局。
- **当前位置栏新增「段」字段**（行号下方，显示当前行所属段号）。
- **文件列表双区**：上区数控程序（MPF），下区 APT 源文件（仅刀具信息）；按 文件名（去 `_I` 后缀）/MPF 头部程序名/apt `PPRINT PROGNAME` 关联，点击一方**高亮另一方**；导入支持 `*.aptsource`。
### 修复
- **NC 代码区横向滚动条**：`cvsb` 改 grid 布局，滚动条铺满代码区宽度（原 pack 被挤到左下角）。
### 测试
- 新增 11 例（抬刀检测/分段/抬刀覆盖、段字段/段导航/段模式渲染与钳制、双区关联、横向滚动条宽度）；测试总数 **104**（tool 15 + 解析 40 + 几何 7 + 界面 42），py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含按段浏览与双区关联，约 10.7 MB），冒烟通过。
### 修改
- **段控制独立栏**：右侧面板改为双列（左：按行定位/搜索定位；右：按段浏览），新增**所有段列表**（点击直接跳转，当前段高亮）。
- **勾选「仅显示当前段」直接展示该段完整刀路**（标记在段末，不再从空轨迹开始）；从头播放先点「复位」；取消勾选恢复全程序刀路。
- **侧栏三区（程序统计/当前位置/刀具）固定于侧栏底部**，默认加载后完整显示；图例独立滚动。
### 打包
- 重新打包 `dist\NCViewer.exe`（段控制栏与侧栏贴底，约 10.7 MB），冒烟通过。
### 修改
- **底部大栏默认抬高**：NC 代码 + 定位/搜索 + 段控制 大栏默认占窗口高度约 35%（原 25%），启动后 sash 一次性定位，用户仍可拖动调整。
### 打包
- 重新打包 `dist\NCViewer.exe`（底部大栏抬高，约 10.7 MB），冒烟通过。
### 修改
- 底部大栏默认高度 35% -> **45%**。
### 打包
- 重新打包 `dist\NCViewer.exe`（底部大栏 45%，约 10.7 MB），冒烟通过。
### 修复
- **底部大栏 45% 实际生效**：启动后 body 尺寸事件会重置 sash（此前 after 一次性设置被覆盖，实际停留在 25%）；改为**启动稳定期（3 秒）内持续重设默认 sash**，稳定期后交还用户自由拖动。
### 打包
- 重新打包 `dist\NCViewer.exe`（45% 修复，约 10.7 MB），冒烟通过。
### 修改
- 底部大栏默认高度改回 **35%**（45% 太高）。
- **段列表随底部大栏高度伸缩**：段列表行 weight 修正（原来挂在标签行），拖高/拖低底部大栏时列表同步调整。
- **底部大栏最小高度 25%**：拖动分界条向下超过 75% 位置时钳回（25% 下限）。
### 打包
- 重新打包 `dist\NCViewer.exe`（35% 默认 + 25% 下限 + 段列表伸缩，约 10.7 MB），冒烟通过。
### 修改
- **颜色图例漂浮于画布右上角**：画布窗口项锚定右上角，不随视图平移/旋转；**每列最多 6 项，超出向左开新列**。
- **侧栏移除滚动容器**：程序统计/当前位置/刀具三栏直接堆叠（无滚动，完整显示）。
### 测试
- 更新图例/侧栏测试（漂浮锚定、平移不随动、两列布局、无滚动容器）；测试总数 **104**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（图例漂浮 + 侧栏精简，约 10.7 MB），冒烟通过。
### 修改
- **刀具栏弹性延伸至侧栏底部**（即底部大栏分界线，顶住分界线）；**剖面图随分界线拖拽缩放**（画布随高度伸缩并自适应重绘）。
### 打包
- 重新打包 `dist\NCViewer.exe`（刀具栏随分界线伸缩，约 10.7 MB），冒烟通过。
### 优化
- **画布交互性能**：滚轮缩放改 `canvas.scale("all")` **原位缩放**（78ms → ~0ms）；平移为原生 C 级 move；旋转内联预计算四元数系数（43ms → 17ms）；投影缓存（四元数/过滤未变时缩放仅做坐标变换）。
- **切段提速**：`_set_segment` 去重渲染（161ms → 48ms）；bbox 缓存（适配不再重复全量投影）。
- **切程序提速**：代码区批量插入（162ms → 119ms），2.5 万行程序整体切换 ~500ms（解析 111 + 代码区 129 + 适配 88 + 分段 59）。
### 打包
- 重新打包 `dist\NCViewer.exe`（性能优化，约 10.7 MB），冒烟通过。
### 修改
- **程序统计 S/F 显示具体值**（如 `S: 5000 · 8000`、`F: 300 · 600 · 1800 · 3000 · 5000`），不再显示范围。
- **统计随段模式变化**：勾选「仅显示勾选段」后，程序统计按勾选段并集重新计算（行程/S/F/G 均为段内数据）。
- **段列表改为多选勾选**（`[x]/[ ]` 标记），「仅显示当前段」改为「**仅显示勾选段**」：过滤为勾选段并集（全选=全程序、全不选=不显示）；切段时全量渲染，**不再残留上一段刀路**。
### 测试
- 新增/更新 3 例（多段并集与统计随段、S/F 具体值、全选/全不选边界）；测试总数 **105**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（统计具体值 + 多段勾选，约 10.7 MB），冒烟通过。
### 新增
- 段控制面板「仅显示勾选段」旁新增**「取消选择」**按钮：一键清除所有段勾选（段模式下即不显示任何段）。
### 打包
- 重新打包 `dist\NCViewer.exe`（取消选择按钮，约 10.7 MB），冒烟通过。
### 修改
- **程序打开后自动适配两次**：立即适配 + 布局稳定后（700ms，sash/窗口定型）再适配一次，保证图像大小始终合适。
### 打包
- 重新打包 `dist\NCViewer.exe`（打开自动适配，约 10.7 MB），冒烟通过。
### 修改
- **兼容 1366x768 / 1920x1080（Win7 全屏 1920x1048）**：侧栏恢复滚动容器（小屏自动滚动，滚轮可达全部模块）；程序统计改**双列紧凑**布局、当前位置字段降为 MONO、刀具剖面画布默认 120px（空间富余时 weight 拉伸）。
- **底部大栏 sash 自适应**：默认 25%~35%（小屏取小值给上区让空间），大屏仍为 35%。
- 1920x1048 下侧栏**无需滚动**完整显示；1366x768 下滚动查看（溢出约 270px）。
### 测试
- 更新/新增 2 例（三区滚动容器、1366x768 滚动可达）；测试总数 **106**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（分辨率兼容，约 10.7 MB），冒烟通过。
### 修改
- **分辨率无关适配**（1366x768 最低 / 1920x1080 优选 / 更高分辨率良好）：
  - 底部大栏 sash 自适应：底部 = max(25%, min(35%, 高度-700))——768p 保底 25%（sash 宽 6px 实为 24.2%）、1080p 约 28% 让出上区、1440p/4K 回到 35%；
  - **取消全屏时画布优先被压缩**：底部钳制绑定到窗口级尺寸事件（布局定型后触发），窗口最小尺寸提至 1280x700 防画布塌缩；
  - 高 DPI 下右侧面板宽度膨胀（固定字符宽按像素放大）：定位/搜索输入框 20→12 字符、段列表 24→18 字符。
  - 实测：4K/1440p/1080p 侧栏无需滚动、768p 滚动可达、1024x640 以下由最小尺寸拦截。
### 打包
- 重新打包 `dist\NCViewer.exe`（分辨率无关适配，约 10.7 MB），冒烟通过。
### 修改
- **程序统计恢复单列布局**（值全宽显示，F/S 具体值不再挤压）。
- **刀具栏回到自然高度**（剖面画布固定 240px）：额外空间由占位行吸收，大屏不再拉伸过高（4K 实测 376px，此前 744px）。
### 打包
- 重新打包 `dist\NCViewer.exe`（统计单列 + 刀具栏定高，约 10.7 MB），冒烟通过。
### 修改
- **侧栏三栏均分拉伸 + 最低高度**：程序统计/当前位置/刀具三行各 `weight=1` 均分剩余空间（4K 实测各约 430/460/410px），各有最低高度（240/300/240px）。
- **当前位置恢复纵向单列**布局（X/Y/Z/S/F/G/行/段 + 本行 共 9 行）。
- 可用高度低于最低高度之和时启用纵向滚动条（1080p/768p 实测滚动可达）。
### 打包
- 重新打包 `dist\NCViewer.exe`（三栏均分+最低高度+单列位置，约 10.7 MB），冒烟通过。
### 修改
- **侧栏高度分配**：程序统计/当前位置**固定自然高度**（各约 300/330px），**刀具栏吸收全部剩余空间**（最小 240px），不足时滚动。
### 打包
- 重新打包 `dist\NCViewer.exe`（固定高度+刀具填充，约 10.7 MB），冒烟通过。
### 修复
- **低分辨率侧栏滚动条滚轮失效**：滚轮事件补充绑定到滚动条自身（原只在画布/内容上），滚动条上滚轮即可滚动；滚动到底后刀具栏完整可见（此前因滚不动导致显示不全）。
### 打包
- 重新打包 `dist\NCViewer.exe`（滚动条滚轮修复，约 10.7 MB），冒烟通过。
### 修复
- **侧栏滚轮全局生效**：改为全局滚轮捕获 + 指针位置判断——指针在侧栏任意子控件（标签/输入框/画布/滚动条）上滚轮均滚动侧栏，其余区域（主画布等）不受干扰。
- **剖面图底部裁切**：内嵌剖面图按画布**实际尺寸**绘制（原布局最小 150px，画布被压缩 104px 时底部 46px 被裁）。
### 打包
- 重新打包 `dist\NCViewer.exe`（滚轮全局化 + 剖面图裁切修复，约 10.7 MB），冒烟通过。
### 修改
- **两侧侧边栏收窄 + 最小宽度**：文件栏 280→220、右栏 1180→446（滚动列画布请求宽固定 200，内容按需撑开）；sash 最小宽度钳制（文件栏≥200、侧栏≥240、右栏≥430，画布优先压缩）。
- **横向滚动条**：文件列表 MPF/APT 共享横向滚动条；右侧定位/搜索与段控制两栏各为双轴滚动容器（内容超出时横/竖滚动条生效）。
- **1080p 底部大栏更高**：预留上区空间 700→620（1080p 底部 30%→35%）。
- **右侧两栏竖向滚动条**：按行定位+搜索定位 与 按段浏览 各带竖向滚动条。
- **修复拉高底部栏后刀具剖面图被遮**：侧栏滚动区域改为内容实际渲染范围（窗口项高度 = max(视口, 内容)，滚动后可完整看到刀具栏）。
### 测试
- 更新文件列表宽度断言 30→24；测试总数 **106**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（收窄+滚动条+刀具可见修复，约 10.7 MB），冒烟通过。
### 修改
- **右侧两栏去除横向滚动**：宽度随内容（完整展示全部元素），仅保留竖向滚动条。
- **全局按钮缩小**：普通按钮内边距 10,4→6,2、强调按钮 12,4→8,2。
- **当前位置表格布局**：XYZ / SFG / 行段 各一行（三列并排），本行全宽——高度 330→176px。
### 打包
- 重新打包 `dist\NCViewer.exe`（按钮缩小 + 位置表格 + 去横向滚动，约 10.7 MB），冒烟通过。
### 修改
- **当前位置表格改为标签行+值行成对**：X/Y/Z 一行标签、下一行数值（值宽 11 字符：9 位数字+小数点+符号），S/F/G、行/段 同理，本行全宽。
- **侧栏最小宽度字体测量**：文件栏 ≥15 字符+空余（实测 201px），右栏 ≥3×11 字符单元（实测 473px）；sash 钳制使用动态测量值。
- **横向滚动条行为**：右栏两列默认宽度=内容（无滚动）；被挤窄时内容保持自然宽度、横向滚动条自动激活可滚。
### 打包
- 重新打包 `dist\NCViewer.exe`（位置标签值表格 + 最小宽度测量 + 挤窄横滚，约 10.7 MB），冒烟通过。
### 新增
- 播放控制条新增**「绘制到结尾」**按钮（复位旁）：一键绘制整条程序全部刀路（轨迹模式直达结尾）。
### 测试
- 新增 1 例（绘制到结尾画出全部刀路）；测试总数 **93**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含绘制到结尾，约 10.7 MB），冒烟通过。
### 修改
- **程序统计/当前位置/刀具三区贴住侧栏底部**（始终可见），滚动区只剩图例。
### 打包
- 重新打包 `dist\NCViewer.exe`（三区贴底，约 10.7 MB），冒烟通过。
### 修改
- **侧栏恢复整体滚动设计**（图例/程序统计/当前位置/刀具四节顺序固定、同容器滚动、各节固定大小，刀具剖面 340px）。
### 打包
- 重新打包 `dist\NCViewer.exe`（侧栏整体滚动，约 10.7 MB），冒烟通过。
### 新增
- **按段浏览**：段 = 刀路从抬刀平面下降到回到抬刀平面的完整循环；抬刀平面自动检测（出现次数 ≥2 的最高 Z 档位，排除偶发高 Z），**支持用户修改并自动重算分段**；面板位于搜索定位栏下方（上一段/下一段/段号跳转/段信息）。
- **段模式（仅显示当前段）**：画布只渲染当前段刀路，段内**播放/单步/复位/绘制到结尾**同步生效（钳制在段内）；关闭后恢复全局。
- **当前位置栏新增「段」字段**（行号下方，显示当前行所属段号）。
- **文件列表双区**：上区数控程序（MPF），下区 APT 源文件（仅刀具信息）；按 文件名（去 `_I` 后缀）/MPF 头部程序名/apt `PPRINT PROGNAME` 关联，点击一方**高亮另一方**；导入支持 `*.aptsource`。
### 修复
- **NC 代码区横向滚动条**：`cvsb` 改 grid 布局，滚动条铺满代码区宽度（原 pack 被挤到左下角）。
### 测试
- 新增 11 例（抬刀检测/分段/抬刀覆盖、段字段/段导航/段模式渲染与钳制、双区关联、横向滚动条宽度）；测试总数 **104**（tool 15 + 解析 40 + 几何 7 + 界面 42），py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含按段浏览与双区关联，约 10.7 MB），冒烟通过。
### 修改
- **段控制独立栏**：右侧面板改为双列（左：按行定位/搜索定位；右：按段浏览），新增**所有段列表**（点击直接跳转，当前段高亮）。
- **勾选「仅显示当前段」直接展示该段完整刀路**（标记在段末，不再从空轨迹开始）；从头播放先点「复位」；取消勾选恢复全程序刀路。
- **侧栏三区（程序统计/当前位置/刀具）固定于侧栏底部**，默认加载后完整显示；图例独立滚动。
### 打包
- 重新打包 `dist\NCViewer.exe`（段控制栏与侧栏贴底，约 10.7 MB），冒烟通过。
### 修改
- **底部大栏默认抬高**：NC 代码 + 定位/搜索 + 段控制 大栏默认占窗口高度约 35%（原 25%），启动后 sash 一次性定位，用户仍可拖动调整。
### 打包
- 重新打包 `dist\NCViewer.exe`（底部大栏抬高，约 10.7 MB），冒烟通过。
### 修改
- 底部大栏默认高度 35% -> **45%**。
### 打包
- 重新打包 `dist\NCViewer.exe`（底部大栏 45%，约 10.7 MB），冒烟通过。
### 修复
- **底部大栏 45% 实际生效**：启动后 body 尺寸事件会重置 sash（此前 after 一次性设置被覆盖，实际停留在 25%）；改为**启动稳定期（3 秒）内持续重设默认 sash**，稳定期后交还用户自由拖动。
### 打包
- 重新打包 `dist\NCViewer.exe`（45% 修复，约 10.7 MB），冒烟通过。
### 修改
- 底部大栏默认高度改回 **35%**（45% 太高）。
- **段列表随底部大栏高度伸缩**：段列表行 weight 修正（原来挂在标签行），拖高/拖低底部大栏时列表同步调整。
- **底部大栏最小高度 25%**：拖动分界条向下超过 75% 位置时钳回（25% 下限）。
### 打包
- 重新打包 `dist\NCViewer.exe`（35% 默认 + 25% 下限 + 段列表伸缩，约 10.7 MB），冒烟通过。
### 修改
- **颜色图例漂浮于画布右上角**：画布窗口项锚定右上角，不随视图平移/旋转；**每列最多 6 项，超出向左开新列**。
- **侧栏移除滚动容器**：程序统计/当前位置/刀具三栏直接堆叠（无滚动，完整显示）。
### 测试
- 更新图例/侧栏测试（漂浮锚定、平移不随动、两列布局、无滚动容器）；测试总数 **104**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（图例漂浮 + 侧栏精简，约 10.7 MB），冒烟通过。
### 修改
- **刀具栏弹性延伸至侧栏底部**（即底部大栏分界线，顶住分界线）；**剖面图随分界线拖拽缩放**（画布随高度伸缩并自适应重绘）。
### 打包
- 重新打包 `dist\NCViewer.exe`（刀具栏随分界线伸缩，约 10.7 MB），冒烟通过。
### 优化
- **画布交互性能**：滚轮缩放改 `canvas.scale("all")` **原位缩放**（78ms → ~0ms）；平移为原生 C 级 move；旋转内联预计算四元数系数（43ms → 17ms）；投影缓存（四元数/过滤未变时缩放仅做坐标变换）。
- **切段提速**：`_set_segment` 去重渲染（161ms → 48ms）；bbox 缓存（适配不再重复全量投影）。
- **切程序提速**：代码区批量插入（162ms → 119ms），2.5 万行程序整体切换 ~500ms（解析 111 + 代码区 129 + 适配 88 + 分段 59）。
### 打包
- 重新打包 `dist\NCViewer.exe`（性能优化，约 10.7 MB），冒烟通过。
### 修改
- **程序统计 S/F 显示具体值**（如 `S: 5000 · 8000`、`F: 300 · 600 · 1800 · 3000 · 5000`），不再显示范围。
- **统计随段模式变化**：勾选「仅显示勾选段」后，程序统计按勾选段并集重新计算（行程/S/F/G 均为段内数据）。
- **段列表改为多选勾选**（`[x]/[ ]` 标记），「仅显示当前段」改为「**仅显示勾选段**」：过滤为勾选段并集（全选=全程序、全不选=不显示）；切段时全量渲染，**不再残留上一段刀路**。
### 测试
- 新增/更新 3 例（多段并集与统计随段、S/F 具体值、全选/全不选边界）；测试总数 **105**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（统计具体值 + 多段勾选，约 10.7 MB），冒烟通过。
### 新增
- 段控制面板「仅显示勾选段」旁新增**「取消选择」**按钮：一键清除所有段勾选（段模式下即不显示任何段）。
### 打包
- 重新打包 `dist\NCViewer.exe`（取消选择按钮，约 10.7 MB），冒烟通过。
### 修改
- **程序打开后自动适配两次**：立即适配 + 布局稳定后（700ms，sash/窗口定型）再适配一次，保证图像大小始终合适。
### 打包
- 重新打包 `dist\NCViewer.exe`（打开自动适配，约 10.7 MB），冒烟通过。
### 修改
- **兼容 1366x768 / 1920x1080（Win7 全屏 1920x1048）**：侧栏恢复滚动容器（小屏自动滚动，滚轮可达全部模块）；程序统计改**双列紧凑**布局、当前位置字段降为 MONO、刀具剖面画布默认 120px（空间富余时 weight 拉伸）。
- **底部大栏 sash 自适应**：默认 25%~35%（小屏取小值给上区让空间），大屏仍为 35%。
- 1920x1048 下侧栏**无需滚动**完整显示；1366x768 下滚动查看（溢出约 270px）。
### 测试
- 更新/新增 2 例（三区滚动容器、1366x768 滚动可达）；测试总数 **106**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（分辨率兼容，约 10.7 MB），冒烟通过。
### 修改
- **分辨率无关适配**（1366x768 最低 / 1920x1080 优选 / 更高分辨率良好）：
  - 底部大栏 sash 自适应：底部 = max(25%, min(35%, 高度-700))——768p 保底 25%（sash 宽 6px 实为 24.2%）、1080p 约 28% 让出上区、1440p/4K 回到 35%；
  - **取消全屏时画布优先被压缩**：底部钳制绑定到窗口级尺寸事件（布局定型后触发），窗口最小尺寸提至 1280x700 防画布塌缩；
  - 高 DPI 下右侧面板宽度膨胀（固定字符宽按像素放大）：定位/搜索输入框 20→12 字符、段列表 24→18 字符。
  - 实测：4K/1440p/1080p 侧栏无需滚动、768p 滚动可达、1024x640 以下由最小尺寸拦截。
### 打包
- 重新打包 `dist\NCViewer.exe`（分辨率无关适配，约 10.7 MB），冒烟通过。
### 修改
- **程序统计恢复单列布局**（值全宽显示，F/S 具体值不再挤压）。
- **刀具栏回到自然高度**（剖面画布固定 240px）：额外空间由占位行吸收，大屏不再拉伸过高（4K 实测 376px，此前 744px）。
### 打包
- 重新打包 `dist\NCViewer.exe`（统计单列 + 刀具栏定高，约 10.7 MB），冒烟通过。
### 修改
- **侧栏三栏均分拉伸 + 最低高度**：程序统计/当前位置/刀具三行各 `weight=1` 均分剩余空间（4K 实测各约 430/460/410px），各有最低高度（240/300/240px）。
- **当前位置恢复纵向单列**布局（X/Y/Z/S/F/G/行/段 + 本行 共 9 行）。
- 可用高度低于最低高度之和时启用纵向滚动条（1080p/768p 实测滚动可达）。
### 打包
- 重新打包 `dist\NCViewer.exe`（三栏均分+最低高度+单列位置，约 10.7 MB），冒烟通过。
### 修改
- **侧栏高度分配**：程序统计/当前位置**固定自然高度**（各约 300/330px），**刀具栏吸收全部剩余空间**（最小 240px），不足时滚动。
### 打包
- 重新打包 `dist\NCViewer.exe`（固定高度+刀具填充，约 10.7 MB），冒烟通过。
### 修复
- **低分辨率侧栏滚动条滚轮失效**：滚轮事件补充绑定到滚动条自身（原只在画布/内容上），滚动条上滚轮即可滚动；滚动到底后刀具栏完整可见（此前因滚不动导致显示不全）。
### 打包
- 重新打包 `dist\NCViewer.exe`（滚动条滚轮修复，约 10.7 MB），冒烟通过。
### 修复
- **侧栏滚轮全局生效**：改为全局滚轮捕获 + 指针位置判断——指针在侧栏任意子控件（标签/输入框/画布/滚动条）上滚轮均滚动侧栏，其余区域（主画布等）不受干扰。
- **剖面图底部裁切**：内嵌剖面图按画布**实际尺寸**绘制（原布局最小 150px，画布被压缩 104px 时底部 46px 被裁）。
### 打包
- 重新打包 `dist\NCViewer.exe`（滚轮全局化 + 剖面图裁切修复，约 10.7 MB），冒烟通过。
### 修改
- **两侧侧边栏收窄 + 最小宽度**：文件栏 280→220、右栏 1180→446（滚动列画布请求宽固定 200，内容按需撑开）；sash 最小宽度钳制（文件栏≥200、侧栏≥240、右栏≥430，画布优先压缩）。
- **横向滚动条**：文件列表 MPF/APT 共享横向滚动条；右侧定位/搜索与段控制两栏各为双轴滚动容器（内容超出时横/竖滚动条生效）。
- **1080p 底部大栏更高**：预留上区空间 700→620（1080p 底部 30%→35%）。
- **右侧两栏竖向滚动条**：按行定位+搜索定位 与 按段浏览 各带竖向滚动条。
- **修复拉高底部栏后刀具剖面图被遮**：侧栏滚动区域改为内容实际渲染范围（窗口项高度 = max(视口, 内容)，滚动后可完整看到刀具栏）。
### 测试
- 更新文件列表宽度断言 30→24；测试总数 **106**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（收窄+滚动条+刀具可见修复，约 10.7 MB），冒烟通过。
### 修改
- **右侧两栏去除横向滚动**：宽度随内容（完整展示全部元素），仅保留竖向滚动条。
- **全局按钮缩小**：普通按钮内边距 10,4→6,2、强调按钮 12,4→8,2。
- **当前位置表格布局**：XYZ / SFG / 行段 各一行（三列并排），本行全宽——高度 330→176px。
### 打包
- 重新打包 `dist\NCViewer.exe`（按钮缩小 + 位置表格 + 去横向滚动，约 10.7 MB），冒烟通过。
### 修改
- **当前位置表格改为标签行+值行成对**：X/Y/Z 一行标签、下一行数值（值宽 11 字符：9 位数字+小数点+符号），S/F/G、行/段 同理，本行全宽。
- **侧栏最小宽度字体测量**：文件栏 ≥15 字符+空余（实测 201px），右栏 ≥3×11 字符单元（实测 473px）；sash 钳制使用动态测量值。
- **横向滚动条行为**：右栏两列默认宽度=内容（无滚动）；被挤窄时内容保持自然宽度、横向滚动条自动激活可滚。
### 打包
- 重新打包 `dist\NCViewer.exe`（位置标签值表格 + 最小宽度测量 + 挤窄横滚，约 10.7 MB），冒烟通过。
- 更新 `README.md`、`docs/技术手册.md`、`docs/开发流程.md`：Win7 构建方式、环境要求、打包注意与 FAQ。
### 测试
- conda python38（Python 3.8.19 + Tk 8.6.9）下全量 38 例通过（green）；GUI 冒烟通过（真实样例文件 25321 刀路段正常加载渲染）。
### 打包
- 用 conda python38 + PyInstaller 5.13.2 重新打包 `dist\NCViewer.exe`（内置 UCRT 与样例文件）。
- Win11 冒烟测试通过；**Win7 实测待用户在真实机器上验证**。
### 修改
- 当前位置「本行」拆成两行：标签行在上、值框整行跨满在下（与 XYZ/SFG/行段 的上下表格形式一致）。
### 测试
- 新增 1 例（本行标签/值框两行布局断言）；测试总数 **107**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含本行两行布局，约 10.7 MB），冒烟通过。
### 修复
- **侧栏最小宽度拖动时不生效**：sash 拖动由 ttk 类绑定处理，默认在实例绑定（钳制）之后执行，钳制值被覆盖；新增 `_class_first_bindtags` 交换 bindtags 使类绑定先跑、sash 先移动、钳制后生效（upper/code_split/body 三处 PanedWindow 统一修复）。
### 测试
- 新增 1 例（模拟 sash 按下+拖过最小值后钳回）；测试总数 **108**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含最小宽度钳制修复，约 10.7 MB），冒烟通过。
### 修复
- **侧栏最窄时内容溢出**：刀具栏内嵌剖面画布此前用 Canvas 默认宽（10cm≈567px@144DPI），把侧栏内容请求宽撑到 587px 超出最小视口 443px；画布改为显式小请求宽（靠 weight 拉伸不变）。
- **ttk.Button 宽度地板**：Tk 8.6.9 的 ttk.Button `width` 默认为空串，产生约 93px(@96DPI)/139px(@144DPI) 的宽度地板（与文本无关），按钮一多面板就臃肿；`theme.apply_theme` 经选项库全局 `*TButton.width=0`，按钮按文本自然取宽（底部右栏自然宽 1014→733@144DPI，显式 width 不受影响）。
### 修改
- **三栏宽度自适应（DPI 无关，按运行时真实控件测量）**：默认宽度 = 内容自然宽（不拥挤），最小宽度 = 内容地板（最窄不溢出）；文件栏最小宽度由 15 字符估算（201px）加宽为列表自然宽（324px@144DPI）；底部右栏最小宽度由写死 479 改为两列均分实测（762px@144DPI）。
- **加载文件后栏宽再适配**：统计侧栏内容随程序变化（S/F 档位列表等），`set_current_file` 后按内容自然宽设置三栏（上限 45% 窗宽）；用户拖过 sash 后不再自动调整。
### 测试
- 新增 4 例（最窄侧栏内容适应、文件栏最小容纳列表自然宽、默认栏宽内容完整、按钮按文本取宽）；测试总数 **112**，py38 / py311 双环境全绿。
### 文档
- `CLAUDE.md`、`docs/技术手册.md`（本地版已同步）：栏宽自适应机制、bindtags 顺序钳制、ttk.Button 宽度地板修复。
### 打包
- 重新打包 `dist\NCViewer.exe`（含栏宽自适应与按钮地板修复，约 10.7 MB），冒烟通过。
### 修复
- **底部右栏两列滚轮不滚动**：全局滚轮分派 `_on_side_wheel_global` 此前只认右侧栏，底部右栏两个滚动列（按行定位/搜索定位、按段浏览）不在分派列表；现按指针所在区域分派到对应滚动列。段列表自身滚轮仍由其类绑定处理（特判避免双重滚动），其滚动条上滚轮滚段列表。
### 测试
- 新增 2 例（底部右栏滚轮滚动、段列表滚轮不带动所在列）；测试总数 **114**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含底部右栏滚轮修复，约 10.7 MB），冒烟通过。
### 修复
- **段列表点击错乱/跳段**：点击勾选时 `_refresh_seg_list_marks` 重建整个列表丢失滚动位置（且 `see()` 强制跳到当前段），两次点击之间列表跳走导致点中段错位（点 20 段选中 23/24 段）；现重建前后恢复滚动位置，列表点击路径不再 see。
- **段模式刀路不显示**：播放/复位等轨迹操作后 `_trace_active` 仍为真，此时勾选段变化走 `_trace_redraw`（按旧已画移动数重绘），新勾选段落在范围外而不显示；现段过滤变化且轨迹激活时改为全量渲染（退出轨迹模式）。
- **退刀刀路带箭头残留**：当前行落在被段过滤掉的移动上时，`_draw_current` 仍画当前段高亮红线与方向箭头；现被过滤的移动不再高亮。
- **多段并集渲染幻影连接线**：同色折线合并此前只看颜色，段过滤间隙处把不相邻的移动连成一条直线；现仅当两移动共享端点（连续）才合并（全量渲染与轨迹增量绘制同步修复）。
- **切换程序时段勾选残留越界崩溃**：勾选集合未随文件清空，新程序段数更少时 `_apply_seg_filter` 索引越界（IndexError）；加载文件时清空勾选，且勾选索引越界忽略。
### 新增
- **NC 代码区行号槽段号标注**：段内行显示 `S<n>`（青色），段外行留空；修改抬刀平面后随分段重算自动重建，当前行/目标行/搜索高亮保持。
### 测试
- 新增 4 例（段列表点击保持滚动、轨迹中改勾选全量渲染、过滤段不高亮、代码区段号标注与重建）；滚轮 2 例改为 winfo_containing 打桩的确定性断言（原受窗口堆叠环境影响）；测试总数 **118**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含按段浏览三项修复与段号标注，约 10.7 MB），冒烟通过。
### 修复
- **「复位+绘制到结尾」大程序卡死**：`_trace_draw_to` 逐移动调用 `canvas.coords` 回写合并折线的全量坐标，大跳变为 O(n²)（2.5 万段程序卡死）；现合并折线在循环末尾统一回写，实测 25321 段「复位+绘制到结尾」约 236ms。
- **十字虚线平移后不铺满**：平移 `canvas.move("all")` 把十字虚线一并移出可视区；现平移时按当前位置重铺到可视区两端（`_draw_crosshair`，独立 `curx` 标签）。
### 修改
- **段边界新语义**：段从抬刀平面上的定位移动（下落前一行）开始，到回升至抬刀平面结束；段间连续划分（上段结束后的移动归入下段）。到平面判定容差收紧为 max(0.05mm, 行程 0.01%)，避免把接近平面的中途移动误判为到平面。
- **十字虚线暗化**：新增 `CUR_LINE_COLOR`（#786e12 暗化黄），当前点仍为亮黄，虚线不再刺眼。
### 测试
- 新增 2 例（coords 批量回写调用数上界、十字虚线平移重铺+暗化）；段边界 3 例解析测试与 2 例 UI 测试更新为新语义；测试总数 **120**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含卡顿修复/段边界新语义/十字虚线修复，约 10.7 MB），冒烟通过。
### 新增
- **画布点击拾取跳转**：左键单击（按下与释放位移 ≤4px，与拖动平移区分）画布内刀路，按**到折线段的最近距离**（≤12px）拾取移动并跳转到对应 NC 行——长直线段中点也可点中；前导跳过段/段过滤掉的段/隐藏的 G0 不参与拾取；轨迹播放中点击按增量绘制跳转（相当于点击调节播放进度）。单次拾取全量扫描 2.5 万段实测约 52ms。
### 测试
- 新增 4 例（点到线段距离纯函数、点击跳转/拖动不触发/空白不变、G0 可见性、段过滤不参与拾取）；测试总数 **124**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含画布点击拾取跳转，约 10.7 MB），冒烟通过。
### 修改
- **方向箭头/当前点/十字虚线固定像素尺寸**：滚轮缩放走 `canvas.scale("all")` 原位缩放，会把标记类图元一起放大；缩放后重绘标记（箭头固定 14px、当前点 6px、十字虚线重铺可视区两端）。
### 新增
- **中键平面旋转（滚转）**：中键按下未吸附到刀路点（空白处）时，拖动绕**画布中心**做平面旋转——画面跟随鼠标绕中心的转角转动，中心固定不动（复用 `compensate_center`）；吸附到刀路点仍为轨道旋转。距中心 10px 内不响应（角度不稳）。
### 测试
- 新增 2 例（缩放后箭头固定尺寸、滚转模式角度/锚点固定/吸附回退轨道）；测试总数 **126**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含箭头固定尺寸与中键滚转，约 10.7 MB），冒烟通过。
### 修改
- 滚转锚点由点击处改为**画布中心**（平面旋转绕画布中心进行，中心固定不动）。
### 新增
- **点击无刀路区域清除当前选择**：画布单击未命中刀路时，清除当前位置状态（十字/当前段高亮/方向箭头、位置字段置 `-`、代码当前行高亮与行号输入框清空）；拖动平移不触发。
### 测试
- 新增 1 例（点空白清除选择的完整状态断言），点击拾取测试语义由"空白不变"更新为"空白清除"；测试总数 **127**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含点空白清除选择，约 10.7 MB），冒烟通过。
### 修改
- **画布点击拾取语义修正**：点击刀路是"跳转查看该行"，不再把轨迹绘制到该行。**播放/暂停的轨迹模式下点击已绘制刀路仅为查看**——代码区显示查看高亮（`viewline` 暗琥珀色）、位置字段显示该行值，**执行位置不动**，续播从停下的地方继续；轨迹模式点空白为"取消查看"（字段回到执行位置），非轨迹模式点空白仍为清除当前选择。
- **画面内没有的刀路不能被选中**：轨迹模式下拾取范围限制为已绘制部分。
### 测试
- 新增 2 例（暂停中点击查看执行位置不动且续播原位继续、未绘制部分不可点中）；测试总数 **129**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含点击拾取语义修正，约 10.7 MB），冒烟通过。
### 新增
- **目标行可清空**：点击代码行选中目标行后，再次点击同一行取消选中；播放控制条「目标行」显示旁新增「清空」按钮（无目标时禁用）。
### 测试
- 新增 1 例（目标行再点取消/清空按钮/按钮状态跟随）；测试总数 **130**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含目标行清空，约 10.7 MB），冒烟通过。
### 优化
- **渲染性能**（25,321 段真实样例实测，面向低配 Win7）：
  - **画布尺寸变化不再全量重绘**：窗口拉伸/任务栏还原/分界条拖动时场景投影未变，仅重贴图例 + 重铺十字虚线——每事件 ~42ms → ~0ms（此前每个 Configure 事件都全量重绘，拉伸时连续触发导致卡顿）；
  - **折线图元复用**：结构（颜色+点数序列）不变时旋转/滚转仅 `coords` 原位更新，免 delete/create——旋转单帧 88ms → 63ms；投影缓存命中渲染 42ms → 17ms；
  - **旋转/滚转刷新合并**：连续 motion 事件经 `after_idle` 攒为一帧，不再积压掉帧；释放时立即最终全量帧；
  - **拖动中圆弧抽稀**：旋转拖动期间 >4 点的移动减半采样，释放后恢复全量（`_drag_coarse`）。
### 测试
- 新增 4 例（尺寸变化不重建图元、旋转复用图元 id 不变、拖动合并刷新、抽稀后释放恢复全量）；测试总数 **134**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含渲染性能优化，约 10.7 MB），冒烟通过。
### 新增
- **文件加载进度弹窗**：打开文件后加载超过 80ms 时弹出模态进度窗（居中于主窗口），分阶段显示 读取 → 解析 → 离散刀路（按移动数推进）→ 载入界面 + 进度条；小文件快速加载不弹窗防闪烁；加载中禁止与主窗口交互（grab），完成自动关闭。读取失败的错误消息延迟到弹窗关闭后统一弹出（避免模态抢占）。2.5 万段样例加载 539ms，弹窗自解析阶段起可见。
### 测试
- 新增 2 例（加载进度单调推进且结束关闭、弹窗延迟创建/更新/销毁机制）；测试总数 **136**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含加载进度弹窗，约 10.7 MB），冒烟通过。
### 修改
- **播放时当前行在代码区居中**：逐行运行（播放/演示/单步/直达）的动画路径用 `yview_moveto` 把当前行保持在 NC 代码区垂直中间（程序开头/结尾不足半屏时自然贴边）；手动跳转/搜索仍是最小滚动 `see`。
### 测试
- 新增 1 例（逐行推进保持居中 + 开头贴顶/结尾贴底/手动跳转不重排）；测试总数 **137**，py38 / py311 双环境全绿。
### 打包
- 重新打包 `dist\NCViewer.exe`（含播放当前行居中，约 10.7 MB），冒烟通过。

---

## [1.0.0] - 2026-08-07

本次会话对项目进行标准化重构、补充工程配置、打包为 EXE，并整理界面。
### 新增
- 新增开发流程规范 `docs/开发流程.md`（TDD、测试、打包、发布检查清单；流程变更亦须维护至此）。
- 建立标准包结构 `src/nc_viewer`，支持 `python -m nc_viewer` 运行。
- 新增工程配置：`pyproject.toml`（构建 + pytest 配置）、`requirements.txt`、`README.md`、`.gitignore`。
- 新增技术手册 `docs/技术手册.md`（概述、安装、使用、架构、算法、测试、FAQ、扩展）。
- 新增 PyInstaller 打包入口 `launcher.py` 与打包脚本 `build_exe.bat`，产物为单文件 `dist/NCViewer.exe`（含样例文件）。
- 新增界面结构测试 `tests/test_viewer_ui.py`（1 例，验证按钮去重）。
### 修改
- 维护 `README.md`：新增「文档」索引（技术手册/开发流程/更改记录）、「打包 EXE」章节，更新测试数量（58 例）与项目结构（补全 `launcher.py`/`build_exe.bat`/`CHANGELOG`/`docs`/新测试）。
- 将 `docs/技术手册.md` 增强为总结性文档：补全目录结构（含 `launcher.py`/`build_exe.bat`/`CHANGELOG`/新测试）、测试覆盖（38 例）、打包说明，并新增「注意事项」章节（开发/运行/使用/打包/文档维护），开头加入相关文档索引。
- 将原扁平文件 `nc_parser.py` / `nc_viewer.py` 重构为包模块）   - `src/nc_viewer/parser.py`：NC 解析（原 `nc_parser.py`）。   - `src/nc_viewer/geometry.py`：配色 / 圆弧离散 / 四元数轨道旋转 / 投影（纯计算，不依赖 Tkinter）。   - `src/nc_viewer/viewer.py`：Tkinter 主窗口（原 `nc_viewer.py`）。   - `src/nc_viewer/__main__.py`：`python -m nc_viewer` 入口。
- 整理界面：移除左侧文件栏中重复的「打开文件…」按钮，文件栏改为纯列表，统一由顶部工具条作为文件入口。
- 样例文件目录定位改为基于包路径向上推断项目根。
### 移除
- 删除旧扁平文件：`nc_parser.py`、`nc_viewer.py`、`test_nc_parser.py`、`test_nc_viewer_math.py`（内容已迁移到包中 `tests/`）。
### 测试
- 新增 `tests/test_viewer_ui.py` 3 例。
- 测试总数：**38**（解析 28 + 几何 7 + 界面结构 3），`py -3.11 -m pytest -q` 全部通过（green）。
### 打包
- 使用 `build_exe.bat` 生成 `dist/NCViewer.exe`（单文件、无控制台、含样例文件，约 29MB）。
- 冒烟测试通过：EXE 启动后进程存活、无崩溃。
---

<!-- 后续更改在此处上方（最近在上）追加记录 -->
